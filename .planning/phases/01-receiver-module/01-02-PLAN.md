---
phase: 01-receiver-module
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - receiver/tetragonreceiver/convert.go
  - receiver/tetragonreceiver/convert_test.go
  - receiver/tetragonreceiver/testdata/events/process_exec.json
  - receiver/tetragonreceiver/testdata/events/process_exit.json
  - receiver/tetragonreceiver/testdata/events/process_kprobe.json
  - receiver/tetragonreceiver/testdata/events/process_tracepoint.json
  - receiver/tetragonreceiver/testdata/events/process_loader.json
  - receiver/tetragonreceiver/testdata/events/process_uprobe.json
  - receiver/tetragonreceiver/testdata/events/process_lsm.json
  - receiver/tetragonreceiver/testdata/events/process_usdt.json
  - receiver/tetragonreceiver/testdata/events/process_throttle.json
  - receiver/tetragonreceiver/testdata/events/rate_limit_info.json
  - receiver/tetragonreceiver/testdata/golden/process_exec.yaml
  - receiver/tetragonreceiver/testdata/golden/process_exit.yaml
  - receiver/tetragonreceiver/testdata/golden/process_kprobe.yaml
  - receiver/tetragonreceiver/testdata/golden/process_tracepoint.yaml
  - receiver/tetragonreceiver/testdata/golden/process_loader.yaml
  - receiver/tetragonreceiver/testdata/golden/process_uprobe.yaml
  - receiver/tetragonreceiver/testdata/golden/process_lsm.yaml
  - receiver/tetragonreceiver/testdata/golden/process_usdt.yaml
  - receiver/tetragonreceiver/testdata/golden/process_throttle.yaml
  - receiver/tetragonreceiver/testdata/golden/rate_limit_info.yaml
  - receiver/tetragonreceiver/testdata/README.md
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-04
  - CONV-05
  - CONV-06
  - CONV-07
  - CONV-08
  - CONV-09
  - CONV-10

must_haves:
  truths:
    - "Each GetEventsResponse becomes exactly one plog.LogRecord with scope name tetragonreceiver"
    - "Log body is protojson.Marshal output with UseProtoNames:true (snake_case fields)"
    - "Timestamp comes from event time field, ObservedTimestamp is receive time"
    - "Severity is INFO for exec/exit/loader, WARN for kprobe/tracepoint/lsm/uprobe/usdt, ERROR for throttle/rate_limit"
    - "Process attributes (binary, arguments, pid, uid, exec_id, cwd) extracted from process field"
    - "Parent attributes extracted when parent is present"
    - "Event-specific attributes extracted (policy_name, action, function_name, subsys, exit status/signal)"
    - "Kubernetes attributes set when pod info present"
    - "All 10 event types produce valid LogRecords without panics"
    - "Golden file tests validate converter output byte-for-byte using plogtest.CompareLogs"
  artifacts:
    - path: "receiver/tetragonreceiver/convert.go"
      provides: "convertEvent function and helpers"
      exports: ["convertEvent"]
      min_lines: 150
    - path: "receiver/tetragonreceiver/convert_test.go"
      provides: "Golden file tests and supplementary attribute tests for all 10 event types"
      min_lines: 100
    - path: "receiver/tetragonreceiver/testdata/events/process_exec.json"
      provides: "Proto-schema-faithful synthetic Tetragon JSON fixture for exec event"
    - path: "receiver/tetragonreceiver/testdata/golden/process_exec.yaml"
      provides: "Golden expected plog.Logs YAML for exec event converter output"
  key_links:
    - from: "receiver/tetragonreceiver/convert.go"
      to: "google.golang.org/protobuf/encoding/protojson"
      via: "protojson.MarshalOptions{UseProtoNames: true}"
      pattern: "UseProtoNames.*true"
    - from: "receiver/tetragonreceiver/convert.go"
      to: "go.opentelemetry.io/collector/pdata/plog"
      via: "plog.NewLogs() and LogRecord creation"
      pattern: "plog\\.NewLogs"
    - from: "receiver/tetragonreceiver/convert.go"
      to: "github.com/cilium/tetragon/api"
      via: "GetEventsResponse event type switch"
      pattern: "GetEventsResponse"
    - from: "receiver/tetragonreceiver/convert_test.go"
      to: "github.com/open-telemetry/opentelemetry-collector-contrib/pkg/golden"
      via: "golden.ReadLogs / golden.WriteLogs for golden file comparison"
      pattern: "golden\\.(ReadLogs|WriteLogs)"
    - from: "receiver/tetragonreceiver/convert_test.go"
      to: "github.com/open-telemetry/opentelemetry-collector-contrib/pkg/pdatatest/plogtest"
      via: "plogtest.CompareLogs for structural comparison"
      pattern: "plogtest\\.CompareLogs"
---

<objective>
Implement the event-to-LogRecord converter covering all 10 Tetragon event types with full attribute extraction, validated by golden file tests.

Purpose: The converter is the core data transformation — each GetEventsResponse becomes a plog.LogRecord with JSON body, severity, timestamps, and extracted attributes for indexing. This is the highest-risk code (protojson format compatibility with OpenObserve). Golden file tests using `pkg/golden` + `plogtest.CompareLogs()` ensure byte-for-byte output fidelity per CONTEXT.md locked decision.
Output: convert.go with convertEvent() function, proto-schema-faithful synthetic test fixtures, golden YAML expected outputs, and passing tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-receiver-module/01-CONTEXT.md
@.planning/phases/01-receiver-module/01-RESEARCH.md
@.planning/phases/01-receiver-module/SPEC-DISCREPANCIES.md
@SPEC.md
@.planning/phases/01-receiver-module/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proto-schema-faithful synthetic Tetragon JSON test fixtures for all 10 event types</name>
  <files>
    receiver/tetragonreceiver/testdata/events/process_exec.json
    receiver/tetragonreceiver/testdata/events/process_exit.json
    receiver/tetragonreceiver/testdata/events/process_kprobe.json
    receiver/tetragonreceiver/testdata/events/process_tracepoint.json
    receiver/tetragonreceiver/testdata/events/process_loader.json
    receiver/tetragonreceiver/testdata/events/process_uprobe.json
    receiver/tetragonreceiver/testdata/events/process_lsm.json
    receiver/tetragonreceiver/testdata/events/process_usdt.json
    receiver/tetragonreceiver/testdata/events/process_throttle.json
    receiver/tetragonreceiver/testdata/events/rate_limit_info.json
    receiver/tetragonreceiver/testdata/README.md
  </files>
  <read_first>
    SPEC.md
    .planning/phases/01-receiver-module/01-CONTEXT.md
    .planning/phases/01-receiver-module/01-RESEARCH.md
  </read_first>
  <action>
Create `receiver/tetragonreceiver/testdata/events/` directory and proto-schema-faithful synthetic JSON fixtures. These MUST be structurally faithful to the Tetragon protobuf schema (`github.com/cilium/tetragon/api v1.6.0`) — field names, types, and nesting must match the proto definitions exactly. Use snake_case field names (proto field names, matching `UseProtoNames: true`).

**IMPORTANT:** These are synthetic fixtures built from the proto schema, NOT hand-invented JSON. Every field name and type must correspond to an actual proto field in `tetragon/api/v1/tetragon/events.proto`. After creating all fixtures, verify each can be unmarshaled via `protojson.Unmarshal` into `tetragonv1.GetEventsResponse` — fix any errors before proceeding.

Each JSON fixture is a `GetEventsResponse` wrapper. The key structures from the Tetragon proto:
- `GetEventsResponse` has a `time` field (RFC3339 timestamp) and a oneof `event` field
- Event-specific fields use the proto field name as JSON key (e.g., `process_exec`, `process_exit`)
- `Process` has: `exec_id` (string), `pid` (UInt32Value -> `{"value": N}`), `uid` (UInt32Value), `cwd`, `binary`, `arguments`, `start_time` (Timestamp), `pod` (Pod)
- `Pod` has: `namespace`, `name`, `container` with `name` field
- UInt32Value wrapper: `{"value": 1234}` in JSON

**process_exec.json** — GetEventsResponse with `process_exec` field:
```json
{
  "process_exec": {
    "process": {
      "exec_id": "a2luZC1jb250cm9sLXBsYW5lOjEyMzQ1",
      "pid": {"value": 1234},
      "uid": {"value": 0},
      "cwd": "/",
      "binary": "/usr/bin/curl",
      "arguments": "https://example.com",
      "start_time": "2026-03-18T10:00:00Z",
      "pod": {
        "namespace": "default",
        "name": "nginx-7b4f8d6c5-x9k2m",
        "container": {
          "name": "nginx"
        }
      },
      "parent_exec_id": "a2luZC1jb250cm9sLXBsYW5lOjk4NzY1"
    },
    "parent": {
      "exec_id": "a2luZC1jb250cm9sLXBsYW5lOjk4NzY1",
      "pid": {"value": 1000},
      "binary": "/bin/bash",
      "cwd": "/home/user"
    }
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:00:00.123456789Z"
}
```

**process_exit.json** — with `status` and `signal` fields:
```json
{
  "process_exit": {
    "process": {
      "exec_id": "exit-test-id-12345",
      "pid": {"value": 5678},
      "uid": {"value": 1000},
      "cwd": "/tmp",
      "binary": "/usr/bin/sleep",
      "arguments": "10"
    },
    "parent": {
      "exec_id": "exit-parent-id-99999",
      "pid": {"value": 5000},
      "binary": "/bin/sh"
    },
    "status": 137,
    "signal": "SIGKILL"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:01:00Z"
}
```

**process_kprobe.json** — with `policy_name`, `action`, `function_name`:
```json
{
  "process_kprobe": {
    "process": {
      "exec_id": "kprobe-test-id-11111",
      "pid": {"value": 2222},
      "binary": "/usr/bin/cat",
      "arguments": "/etc/shadow",
      "cwd": "/"
    },
    "policy_name": "file-access-monitor",
    "action": "KPROBE_ACTION_POST",
    "function_name": "security_file_open"
  },
  "node_name": "worker-2",
  "time": "2026-03-18T10:02:00Z"
}
```

**process_tracepoint.json** — with `subsys`, `event`, `policy_name`, `action`:
```json
{
  "process_tracepoint": {
    "process": {
      "exec_id": "tp-test-id-33333",
      "pid": {"value": 3333},
      "binary": "/usr/bin/mount",
      "cwd": "/"
    },
    "policy_name": "syscall-monitor",
    "action": "KPROBE_ACTION_POST",
    "subsys": "raw_syscalls",
    "event": "sys_enter"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:03:00Z"
}
```

**process_loader.json** — minimal, no policy fields:
```json
{
  "process_loader": {
    "process": {
      "exec_id": "loader-test-id-44444",
      "pid": {"value": 4444},
      "binary": "/usr/lib/ld-linux.so",
      "cwd": "/"
    },
    "path": "/usr/lib/libc.so.6"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:04:00Z"
}
```

**process_uprobe.json** — with `policy_name`, `action`, `function_name`:
```json
{
  "process_uprobe": {
    "process": {
      "exec_id": "uprobe-test-id-55555",
      "pid": {"value": 5555},
      "binary": "/usr/bin/python3",
      "cwd": "/app"
    },
    "policy_name": "ssl-monitor",
    "action": "KPROBE_ACTION_POST",
    "function_name": "SSL_write"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:05:00Z"
}
```

**process_lsm.json** — with `policy_name`, `action`, `function_name`:
```json
{
  "process_lsm": {
    "process": {
      "exec_id": "lsm-test-id-66666",
      "pid": {"value": 6666},
      "binary": "/usr/bin/chmod",
      "arguments": "777 /etc/passwd",
      "cwd": "/"
    },
    "policy_name": "lsm-file-audit",
    "action": "KPROBE_ACTION_POST",
    "function_name": "file_permission"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:06:00Z"
}
```

**process_usdt.json** — with `policy_name`, `action`:
```json
{
  "process_usdt": {
    "process": {
      "exec_id": "usdt-test-id-77777",
      "pid": {"value": 7777},
      "binary": "/usr/sbin/mysqld",
      "cwd": "/var/lib/mysql"
    },
    "policy_name": "mysql-query-trace",
    "action": "KPROBE_ACTION_POST"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:07:00Z"
}
```

**process_throttle.json** — throttle event (no process.pod usually):
```json
{
  "process_throttle": {
    "type": "THROTTLE_START",
    "cgroup": "/system.slice/docker.service"
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:08:00Z"
}
```

**rate_limit_info.json** — rate limit info (no process field at all):
```json
{
  "rate_limit_info": {
    "number_of_dropped_process_events": 42
  },
  "node_name": "worker-1",
  "time": "2026-03-18T10:09:00Z"
}
```

**testdata/README.md** — Document the nature and provenance of fixtures:
```markdown
# Test Data

## Event Fixtures (`events/`)

Proto-schema-faithful synthetic Tetragon event JSON fixtures for converter unit tests.
Format: protojson (snake_case field names, matching `protojson.MarshalOptions{UseProtoNames: true}`).

These fixtures are constructed from the `github.com/cilium/tetragon/api v1.6.0` protobuf
schema to exercise all converter code paths. Every field name and type corresponds to an
actual proto field in `tetragon/api/v1/tetragon/events.proto`.

**Validation:** Each fixture is verified to unmarshal via `protojson.Unmarshal` into
`tetragonv1.GetEventsResponse` as part of the test suite.

**To regenerate from a live Tetragon instance:**
```
tetra getevents -o json | head -100 > captured_events.jsonl
# Then split by event type into individual files
```

## Golden Files (`golden/`)

Expected plog.Logs YAML output from the converter, managed by `pkg/golden`.
To update golden files after intentional converter changes, run:
```
UPDATE_GOLDEN=true go test -run TestConvertEvent_Golden ./...
```
```

After creating fixtures, write a small Go test (or add to convert_test.go setup) that iterates all JSON files in `testdata/events/` and verifies `protojson.Unmarshal` succeeds for each. Fix any JSON structure issues revealed by unmarshal errors — the proto definitions are the source of truth.
  </action>
  <verify>
    <automated>ls receiver/tetragonreceiver/testdata/events/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - receiver/tetragonreceiver/testdata/events/ contains exactly 10 JSON files
    - Each file name matches: process_exec.json, process_exit.json, process_kprobe.json, process_tracepoint.json, process_loader.json, process_uprobe.json, process_lsm.json, process_usdt.json, process_throttle.json, rate_limit_info.json
    - receiver/tetragonreceiver/testdata/README.md documents these as "Proto-schema-faithful synthetic" fixtures with regeneration instructions
    - process_exec.json contains `"pod"` with `"namespace"`, `"name"`, `"container"` fields (for k8s attribute tests)
    - process_exec.json contains `"parent"` field (for parent attribute tests)
    - process_kprobe.json contains `"policy_name"`, `"action"`, `"function_name"` fields
    - process_exit.json contains `"status"` and `"signal"` fields
    - process_tracepoint.json contains `"subsys"` and `"event"` fields
    - rate_limit_info.json does NOT contain a `"process"` field (rate_limit has no process)
  </acceptance_criteria>
  <done>10 proto-schema-faithful synthetic JSON fixtures cover all Tetragon event types, verified to unmarshal via protojson, with documentation on provenance and regeneration instructions</done>
</task>

<task type="auto">
  <name>Task 2: Implement convert.go and convert_test.go with golden file validation</name>
  <files>
    receiver/tetragonreceiver/convert.go
    receiver/tetragonreceiver/convert_test.go
    receiver/tetragonreceiver/testdata/golden/process_exec.yaml
    receiver/tetragonreceiver/testdata/golden/process_exit.yaml
    receiver/tetragonreceiver/testdata/golden/process_kprobe.yaml
    receiver/tetragonreceiver/testdata/golden/process_tracepoint.yaml
    receiver/tetragonreceiver/testdata/golden/process_loader.yaml
    receiver/tetragonreceiver/testdata/golden/process_uprobe.yaml
    receiver/tetragonreceiver/testdata/golden/process_lsm.yaml
    receiver/tetragonreceiver/testdata/golden/process_usdt.yaml
    receiver/tetragonreceiver/testdata/golden/process_throttle.yaml
    receiver/tetragonreceiver/testdata/golden/rate_limit_info.yaml
  </files>
  <read_first>
    receiver/tetragonreceiver/config.go
    receiver/tetragonreceiver/factory.go
    receiver/tetragonreceiver/testdata/events/process_exec.json
    receiver/tetragonreceiver/testdata/events/process_kprobe.json
    receiver/tetragonreceiver/testdata/events/rate_limit_info.json
    SPEC.md
    .planning/phases/01-receiver-module/01-RESEARCH.md
  </read_first>
  <action>
Create `receiver/tetragonreceiver/convert.go`:

Package `tetragonreceiver`. Imports: `tetragonv1 "github.com/cilium/tetragon/api/v1/tetragon"`, `plog`, `pcommon`, `protojson`, `time`.

**Package-level var:**
```go
var jsonMarshaler = protojson.MarshalOptions{UseProtoNames: true}
```
This MUST be `UseProtoNames: true` — without it, protojson uses camelCase which breaks OpenObserve queries.

**Main function: `convertEvent(resp *tetragonv1.GetEventsResponse) plog.Logs`**

1. Create `plog.NewLogs()`, append ResourceLogs, ScopeLogs with `Scope().SetName("tetragonreceiver")`, append LogRecord.

2. **Timestamp (CONV-03):** Parse `resp.GetTime()` (a `*timestamppb.Timestamp`). Call `lr.SetTimestamp(pcommon.NewTimestampFromTime(resp.GetTime().AsTime()))`. Set `lr.SetObservedTimestamp(pcommon.NewTimestampFromTime(time.Now()))`.

3. **Body (CONV-02):** `body, err := jsonMarshaler.Marshal(resp)` — on error, set body to error string. `lr.Body().SetStr(string(body))`.

4. **Severity (CONV-04):** Type-switch on `resp.GetEvent()`:
   - `*tetragonv1.GetEventsResponse_ProcessExec`, `*tetragonv1.GetEventsResponse_ProcessExit`, `*tetragonv1.GetEventsResponse_ProcessLoader`: `SeverityNumberInfo` / `"INFO"`
   - `*tetragonv1.GetEventsResponse_ProcessKprobe`, `*tetragonv1.GetEventsResponse_ProcessTracepoint`, `*tetragonv1.GetEventsResponse_ProcessLsm`, `*tetragonv1.GetEventsResponse_ProcessUprobe`, `*tetragonv1.GetEventsResponse_ProcessUsdt`: `SeverityNumberWarn` / `"WARN"`
   - `*tetragonv1.GetEventsResponse_ProcessThrottle`, `*tetragonv1.GetEventsResponse_RateLimitInfo`: `SeverityNumberError` / `"ERROR"`

5. **Static attributes (CONV-05):** `attrs.PutStr("event.domain", "tetragon")` and `attrs.PutStr("event.name", eventTypeName(resp))`.

6. **eventTypeName helper:** Type-switch returning: `"process_exec"`, `"process_exit"`, `"process_kprobe"`, `"process_tracepoint"`, `"process_loader"`, `"process_uprobe"`, `"process_lsm"`, `"process_usdt"`, `"process_throttle"`, `"rate_limit_info"`, default `"unknown"`.

7. **extractProcess helper:** Type-switch to get the `Process` from each event type. `ProcessExec` has `GetProcessExec().GetProcess()`, `ProcessExit` has `GetProcessExit().GetProcess()`, etc. `ProcessThrottle` and `RateLimitInfo` return nil (no process field).

8. **Process attributes (CONV-06):** If process is non-nil:
   - `attrs.PutStr("tetragon.process.binary", proc.GetBinary())`
   - `attrs.PutStr("tetragon.process.arguments", proc.GetArguments())`
   - `attrs.PutInt("tetragon.process.pid", int64(proc.GetPid().GetValue()))` — safe: nil UInt32Value.GetValue() returns 0
   - `attrs.PutInt("tetragon.process.uid", int64(proc.GetUid().GetValue()))`
   - `attrs.PutStr("tetragon.process.exec_id", proc.GetExecId())`
   - `attrs.PutStr("tetragon.process.cwd", proc.GetCwd())`

9. **Parent attributes (CONV-07):** Extract parent via similar type-switch (`GetProcessExec().GetParent()`, etc.). If parent non-nil:
   - `attrs.PutStr("tetragon.parent.binary", parent.GetBinary())`
   - `attrs.PutInt("tetragon.parent.pid", int64(parent.GetPid().GetValue()))`
   - `attrs.PutStr("tetragon.parent.exec_id", parent.GetExecId())`

10. **Event-specific attributes (CONV-08):** Type-switch:
    - `ProcessKprobe`: `attrs.PutStr("tetragon.policy_name", e.GetPolicyName())`, `attrs.PutStr("tetragon.action", e.GetAction().String())`, `attrs.PutStr("tetragon.function_name", e.GetFunctionName())`
    - `ProcessTracepoint`: `attrs.PutStr("tetragon.policy_name", ...)`, `attrs.PutStr("tetragon.action", ...)`, `attrs.PutStr("tetragon.subsys", e.GetSubsys())`, `attrs.PutStr("tetragon.event", e.GetEvent())`
    - `ProcessLsm`: `attrs.PutStr("tetragon.policy_name", ...)`, `attrs.PutStr("tetragon.action", ...)`, `attrs.PutStr("tetragon.function_name", e.GetFunctionName())`
    - `ProcessUprobe`: `attrs.PutStr("tetragon.policy_name", ...)`, `attrs.PutStr("tetragon.action", ...)`, `attrs.PutStr("tetragon.function_name", e.GetFunctionName())`
    - `ProcessUsdt`: `attrs.PutStr("tetragon.policy_name", ...)`, `attrs.PutStr("tetragon.action", ...)`
    - `ProcessExit`: `attrs.PutInt("tetragon.exit.status", int64(e.GetStatus()))`, `attrs.PutStr("tetragon.exit.signal", e.GetSignal())`

11. **Kubernetes attributes (CONV-09):** If process non-nil and `proc.GetPod()` non-nil:
    - `attrs.PutStr("k8s.namespace.name", pod.GetNamespace())`
    - `attrs.PutStr("k8s.pod.name", pod.GetName())`
    - If `pod.GetContainer()` non-nil: `attrs.PutStr("k8s.container.name", pod.GetContainer().GetName())`

**convert_test.go:**

**Primary validation: Golden file tests (per CONTEXT.md locked decision).**

Use `pkg/golden` (`golden.ReadLogs`/`golden.WriteLogs`) and `plogtest.CompareLogs()` as the primary converter output validation strategy. This ensures protojson body output matches captured reference JSON byte-for-byte.

```go
import (
    "github.com/open-telemetry/opentelemetry-collector-contrib/pkg/golden"
    "github.com/open-telemetry/opentelemetry-collector-contrib/pkg/pdatatest/plogtest"
)

func TestConvertEvent_Golden(t *testing.T) {
    tests := []struct {
        name       string
        fixture    string
        goldenFile string
    }{
        {"process_exec", "testdata/events/process_exec.json", "testdata/golden/process_exec.yaml"},
        {"process_exit", "testdata/events/process_exit.json", "testdata/golden/process_exit.yaml"},
        {"process_kprobe", "testdata/events/process_kprobe.json", "testdata/golden/process_kprobe.yaml"},
        {"process_tracepoint", "testdata/events/process_tracepoint.json", "testdata/golden/process_tracepoint.yaml"},
        {"process_loader", "testdata/events/process_loader.json", "testdata/golden/process_loader.yaml"},
        {"process_uprobe", "testdata/events/process_uprobe.json", "testdata/golden/process_uprobe.yaml"},
        {"process_lsm", "testdata/events/process_lsm.json", "testdata/golden/process_lsm.yaml"},
        {"process_usdt", "testdata/events/process_usdt.json", "testdata/golden/process_usdt.yaml"},
        {"process_throttle", "testdata/events/process_throttle.json", "testdata/golden/process_throttle.yaml"},
        {"rate_limit_info", "testdata/events/rate_limit_info.json", "testdata/golden/rate_limit_info.yaml"},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            raw, err := os.ReadFile(tt.fixture)
            require.NoError(t, err)
            var resp tetragonv1.GetEventsResponse
            require.NoError(t, protojson.Unmarshal(raw, &resp))

            got := convertEvent(&resp)

            // Golden file update mode: UPDATE_GOLDEN=true go test -run TestConvertEvent_Golden ./...
            golden.WriteLogs(t, tt.goldenFile, got)

            expected, err := golden.ReadLogs(tt.goldenFile)
            require.NoError(t, err)

            // Compare with IgnoreObservedTimestamp since ObservedTimestamp is time.Now()
            err = plogtest.CompareLogs(expected, got, plogtest.IgnoreObservedTimestamp())
            require.NoError(t, err)
        })
    }
}
```

**Initial golden file generation:** On the FIRST run, use `UPDATE_GOLDEN=true go test -run TestConvertEvent_Golden ./...` to generate the golden YAML files into `testdata/golden/`. Then run tests again WITHOUT `UPDATE_GOLDEN` to verify they pass. Review the generated golden files to confirm correctness (check severity, attributes, body format).

Note: `golden.WriteLogs(t, path, logs)` only writes when `UPDATE_GOLDEN=true` env var is set. When not set, it's a no-op. Check the actual `pkg/golden` API — it may use `golden.WriteLogsIfUpdateGolden(t, path, logs)` or similar. Adapt to the actual API.

**Supplementary attribute tests (keep these alongside golden tests for targeted debugging):**

- `TestConvertEvent_BodySnakeCase`: Load process_exec.json, convert, check body string contains `"process_exec"` (snake_case) and does NOT contain `"processExec"` (camelCase) — validates the critical UseProtoNames setting
- `TestConvertEvent_ThrottleNoProcess`: Load process_throttle.json, verify `tetragon.process.binary` attribute is absent
- `TestConvertEvent_RateLimitNoProcess`: Load rate_limit_info.json, verify `tetragon.process.binary` attribute is absent

**Fixture validation test:**

```go
func TestFixtures_Unmarshal(t *testing.T) {
    files, err := filepath.Glob("testdata/events/*.json")
    require.NoError(t, err)
    require.Len(t, files, 10, "expected 10 event fixtures")
    for _, f := range files {
        t.Run(filepath.Base(f), func(t *testing.T) {
            raw, err := os.ReadFile(f)
            require.NoError(t, err)
            var resp tetragonv1.GetEventsResponse
            require.NoError(t, protojson.Unmarshal(raw, &resp), "fixture must unmarshal into GetEventsResponse")
        })
    }
}
```

**Execution order:**
1. Implement convert.go
2. Write convert_test.go with all tests
3. Run `UPDATE_GOLDEN=true go test -run TestConvertEvent_Golden ./...` to generate golden files
4. Review generated golden YAML files for correctness
5. Run `go test -race -v ./...` to verify all tests pass without update mode
  </action>
  <verify>
    <automated>cd receiver/tetragonreceiver && go test -race -v -run "TestConvert|TestFixtures" ./... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - receiver/tetragonreceiver/convert.go contains `var jsonMarshaler = protojson.MarshalOptions{UseProtoNames: true}`
    - receiver/tetragonreceiver/convert.go contains `func convertEvent(resp *tetragonv1.GetEventsResponse) plog.Logs`
    - receiver/tetragonreceiver/convert.go contains `Scope().SetName("tetragonreceiver")`
    - receiver/tetragonreceiver/convert.go contains `attrs.PutStr("event.domain", "tetragon")`
    - receiver/tetragonreceiver/convert.go contains `attrs.PutStr("event.name"`
    - receiver/tetragonreceiver/convert.go contains `tetragon.process.binary`
    - receiver/tetragonreceiver/convert.go contains `tetragon.parent.binary`
    - receiver/tetragonreceiver/convert.go contains `k8s.namespace.name`
    - receiver/tetragonreceiver/convert.go contains `k8s.pod.name`
    - receiver/tetragonreceiver/convert.go contains `tetragon.exit.status`
    - receiver/tetragonreceiver/convert.go contains `tetragon.function_name`
    - receiver/tetragonreceiver/convert.go contains `tetragon.subsys`
    - receiver/tetragonreceiver/convert_test.go contains `TestConvertEvent_Golden`
    - receiver/tetragonreceiver/convert_test.go contains `golden.ReadLogs` or `golden.WriteLogs`
    - receiver/tetragonreceiver/convert_test.go contains `plogtest.CompareLogs`
    - receiver/tetragonreceiver/convert_test.go contains `plogtest.IgnoreObservedTimestamp`
    - receiver/tetragonreceiver/convert_test.go contains `TestConvertEvent_BodySnakeCase`
    - receiver/tetragonreceiver/convert_test.go contains `TestFixtures_Unmarshal`
    - receiver/tetragonreceiver/testdata/golden/ contains 10 YAML files
    - `cd receiver/tetragonreceiver && go test -race ./...` exits 0 with all convert tests passing
  </acceptance_criteria>
  <done>convertEvent() handles all 10 Tetragon event types with golden file validation via pkg/golden + plogtest.CompareLogs(expected, got, plogtest.IgnoreObservedTimestamp()), supplementary snake_case guard test, and fixture unmarshal validation</done>
</task>

</tasks>

<verification>
1. `cd receiver/tetragonreceiver && go test -race -v -run "TestConvert|TestFixtures" ./...` passes all tests
2. `grep -c "UseProtoNames" receiver/tetragonreceiver/convert.go` returns at least 1
3. `ls receiver/tetragonreceiver/testdata/golden/*.yaml | wc -l` returns 10
4. Golden files contain expected plog.Logs YAML with correct severity, attributes, and body
5. Body JSON uses snake_case field names (not camelCase) — validated by TestConvertEvent_BodySnakeCase
6. All 10 event types produce one LogRecord each with correct severity via golden comparison
7. Process/parent/k8s/event-specific attributes correctly extracted (validated by golden files)
</verification>

<success_criteria>
- convertEvent() produces one LogRecord per GetEventsResponse for all 10 event types
- Body is protojson with UseProtoNames:true (snake_case, matching Tetragon JSON export)
- Golden file tests validate converter output using pkg/golden ReadLogs/WriteLogs + plogtest.CompareLogs (per CONTEXT.md locked decision)
- 10 golden YAML files in testdata/golden/ capture expected output for each event type
- Severity mapping: INFO for exec/exit/loader, WARN for kprobe/tracepoint/lsm/uprobe/usdt, ERROR for throttle/rate_limit
- Synthetic fixtures are proto-schema-faithful (verified by TestFixtures_Unmarshal)
- All tests pass with race detector
</success_criteria>

<output>
After completion, create `.planning/phases/01-receiver-module/01-02-SUMMARY.md`
</output>
