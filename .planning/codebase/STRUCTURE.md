# Directory Structure

## Status

**No application code exists yet.** Current structure is GSD framework scaffolding only.

## Current Layout

```
otel-collector-tetragon/
  .claude/              # Claude Code configuration and GSD framework
  .devcontainer/        # Dev container configuration
  .planning/            # GSD planning artifacts
    codebase/           # This directory — codebase analysis
  SPEC.md               # Project specification (empty)
```

## Key Observations

- Repository is greenfield — no source code, build files, or dependencies
- `.devcontainer/` present but may need language-specific tooling (Go, etc.)
- `SPEC.md` exists but is empty — project requirements not yet defined
