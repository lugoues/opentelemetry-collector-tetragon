# Architecture

## Status

**No application code exists yet.** This repository contains only GSD framework scaffolding, a `.devcontainer` configuration, and an empty `SPEC.md`.

Architecture analysis will be meaningful after initial implementation phases.

## Intended Domain

Based on the repository name `otel-collector-tetragon`, this project likely involves:
- **OpenTelemetry Collector** — vendor-agnostic telemetry data collection/processing/export
- **Tetragon** — Cilium's eBPF-based security observability and runtime enforcement tool
- Integration between the two systems (custom receiver, processor, or exporter)

## Entry Points

None yet.

## Data Flow

To be determined during project initialization.
